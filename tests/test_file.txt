mi_programa patito

mi_funcion recursiva(oops : entero) : entero{
    condicion (oops <= 10){
        imprimir(oops)
        regresar(oops)
    } alternativa {
        regresar(5 + recursiva(oops - 15))
    }
}

### Se ignora
iniciar() {
    variable purt : entero
    purt = 50
    imprimir(recursiva(purt))
    imprimir("Hola Mundo")
}